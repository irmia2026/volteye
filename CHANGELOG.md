# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 约定。

## [0.2.0] - 2026-08-22

结构化采集与生产级导出的大版本，另含磁盘占用与数据目录探测的重要修复。

### 新增

- **工单结构化采集**：新增 `capture` 包，可插拔 `Extractor` 接口 + 数据驱动的 `bracketkv` 解析器（括号深度匹配、键名规范化、值内标点免疫）
- **格式定义可配置**：`formats` 表持久化签名正则/括号对/字段别名表/分类链标记，TUI「格式」面板支持增删改/启停，保存前编译验证；内置格式 seed 版本化迁移，老库自动升级
- **内置 5 种南网格式**：csg-a 故障单（DY）、csg-b/c/d 工作单（SL/0700，`[]`/`【】`/`()` 三种括号）、csg-e 报障单（KF）；签名锚定带括号字段名，覆盖【海南电网】前缀与无前缀转发
- **工单落盘**：`work_orders` 表（单号唯一去重、消息溯源、原文保留）；`parse_errors` 表记录"签名命中但解析失败"，格式漂移不再静默丢单
- **工单面板**：列表 + 详情 + 解析异常页 + 关键词过滤 + 导出
- **生产级 Excel 导出**：标题/元信息行、品牌色表头、冻结窗格、自动筛选、斑马纹、特急/紧急条件色、自动行高、按内容调列宽、电话与用户编号文本格式、是否完成列人工填写提示色；按业务类型分 sheet 且顺序固定
- **追加导出**：`a` 键追加到既有 VoltEye 导出文件，工作单号去重，人工编辑列原样保留
- **联系人显示优化**：联系人仅为称谓（先生/女士/客户等）时合并用户名称显示为"姓名(称谓)"
- **群管理搜索**：`/` 按群名/备注/wxid 过滤

### 修复

- **磁盘占用减半**：加密副本从常驻改为解密后即删的临时文件，工作目录从微信库体积的 2 倍降到 1 倍；修复 enc WAL 副本永不删除的泄漏；启动时清理历史残留
- **m1 调试程序只同步不解析**：接入格式注册表，避免用错二进制导致消息"采了但没解析"
- **4.x 数据目录探测**：读取 `%APPDATA%\Tencent\xwechat\config\*.ini`（4.x 真正的位置记录）与注册表，覆盖用户目录直下/空格变体等自定义位置；候选目录必须存在真实消息库才采纳

## [0.1.0] - 2026-08-06

首个可用版本（M0~M5 及后续修复）。

### 新增

- 微信 4.x 密钥提取（DLL 内部密钥候选 + 进程内存扫描 + page-1 HMAC 校验）
- SQLCipher 数据库解密与 WCDB 多代际 WAL 回放（仅保留当前 salt 代的帧）
- 增量采集器：按 `(create_time, sort_seq, local_id)` 游标轮询，多分片库支持，幂等去重
- Bubble Tea TUI：总览/群管理/消息流/规则/导出/设置/日志面板，冷色极简设计
- 关键词/正则规则引擎与消息打标
- XLSX 导出（流式写入）、保留期与容量上限清理（清理前自动归档）
- 群备注名、历史回填、采集间隔/自启动等设置项
- 系统托盘模式（`-tray`）：关窗后台运行，托盘菜单重开窗口/退出
- 数据目录锚定 exe 位置，双击/任意工作目录运行共用一份数据
